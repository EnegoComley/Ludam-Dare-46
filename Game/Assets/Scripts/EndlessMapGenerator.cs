using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class EndlessMapGenerator : MonoBehaviour
{
    // Start is called before the first frame update
    public TileBase[] HWallTiles = new TileBase[6];
    public TileBase[] HDoorTiles = new TileBase[6];
    public TileBase[] HNoTiles = new TileBase[6];
    public TileBase[] VWallTiles = new TileBase[6];
    public TileBase[] VDoorTiles = new TileBase[6];
    public TileBase[] VNoTiles = new TileBase[6];


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
